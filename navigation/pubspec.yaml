name: navigation
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  core:
    path: ../core
  core_ui:
    path: ../core_ui

  auto_route: ^10.1.2

dev_dependencies:
  auto_route_generator: ^10.2.4
  build_runner: ^2.7.1
  injectable_generator: ^2.8.1
